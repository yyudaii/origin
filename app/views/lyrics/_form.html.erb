<%= form_with(model: lyric, local: true) do |form| %>
  <% if lyric.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lyric.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% lyric.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :lyrics %>
    <%= form.text_area :lyrics %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
