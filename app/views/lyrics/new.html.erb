<!DOCTYPE html>
<html>
<head>
  <title>新しい歌詞を投稿する</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">仮タイトル</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'ホーム', root_path, class: 'nav-link' %> <!-- ホームへのリンク -->
        </li>
        <li class="nav-item">
          <%= link_to '新しい投稿', new_lyric_path, class: 'nav-link' %> <!-- 新しい投稿へのリンク -->
        </li>
      </ul>
    </div>
  </nav>

  <div class="container mt-5">
    <h1>新しい歌詞を投稿する</h1>
    <%= form_with(model: new_lyric_path, local: true, html: { class: 'form' }) do |form| %>
      <div class="form-group">
        <%= form.label :title, "曲名" %>
        <%= form.text_field :title, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label :lyric, "歌詞" %>
        <%= form.text_area :lyric, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label :image, "ジャケット画像" %>
        <%= form.file_field :image, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.submit "投稿する", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>

  <footer class="footer mt-auto py-3 bg-light">
    <div class="container">
      <span class="text-muted"><a href="#">トップに戻る</a></span>
    </div>
  </footer>
</body>
</html>
