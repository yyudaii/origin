<!DOCTYPE html>
<html>
<head>
  <title>新しい歌詞を投稿する</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">仮タイトル</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="#">ホーム</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">リンク</a>
        </li>
      </ul>
    </div>
  </nav>

  <div class="container mt-5">
    <h1>新しい歌詞を投稿する</h1>
    <%= form_with(url: your_form_path, local: true, html: { class: 'form' }) do |form| %>
      <div class="form-group">
        <%= form.label :name, "名前" %>
        <%= form.text_field :name, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label :lyrics, "歌詞" %>
        <%= form.text_area :lyrics, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label :cover_image, "ジャケット画像" %>
        <%= form.file_field :cover_image, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.submit "投稿する", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>

  <footer class="footer mt-auto py-3 bg-light">
    <div class="container">
      <span class="text-muted"><a href="#">トップに戻る</a></span>
    </div>
  </footer>
</body>
</html>
